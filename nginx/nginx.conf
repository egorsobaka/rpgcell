events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Логи
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Основная конфигурация для lejo.ru
    server {
        listen 80;
        server_name lejo.ru www.lejo.ru;

        # Перенаправление на HTTPS (если нужен SSL)
        # return 301 https://$server_name$request_uri;
    }

    # HTTPS сервер (если есть SSL сертификаты)
    # server {
    #     listen 443 ssl http2;
    #     server_name lejo.ru www.lejo.ru;
    #
    #     ssl_certificate /etc/nginx/ssl/lejo.ru.crt;
    #     ssl_certificate_key /etc/nginx/ssl/lejo.ru.key;
    #
    #     # SSL конфигурация
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;
    #
    #     # Остальная конфигурация
    #     include /etc/nginx/conf.d/lejo.ru.conf;
    # }
}